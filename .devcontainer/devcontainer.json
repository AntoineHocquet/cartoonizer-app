{
    "name": "Cartoonizer App",
    "image": "mcr.microsoft.com/devcontainers/python:3.10",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgl1 && pip install -r requirements.txt"
}